# Python dependencies for the material pipeline.
#
# Copyright (C) 2026 queep-3-arena contributors. GPLv2, see LICENSE.
#
#   python -m venv assets/ml/venv
#   assets/ml/venv/Scripts/pip install -r tools/cosmos/requirements.txt
#
# Python 3.11. Not 3.13: numpy 1.26.4 has no cp313 wheel, and numpy 2 is not what
# Cosmos was tested against.
#
# This is upstream's `requirements.txt` minus four things and plus nothing:
#
#   transformer-engine  no Windows build. tools/cosmos/te_shim/ supplies the three
#                       functions the diffusion path calls -- see D-091.
#   megatron-core       does not pip-install on Windows for two unrelated reasons:
#                       its sdist contains a directory named `pytorch:24.07`, which
#                       NTFS will not create, and its setup.py shells out to
#                       `python3` to build a training-only C++ extension. Install it
#                       by hand: download the sdist, extract skipping the colon
#                       paths, create `requirements/win/requirements.txt` empty, set
#                       PY_ENV=win, empty `ext_modules` in setup.py, then
#                       `pip install --no-deps --no-build-isolation .`
#   decord              video decoding, training-only, no Windows wheel
#   ipdb                a debugger
#
# torch has to come from NVIDIA's index or it arrives CPU-only:
#   pip install torch==2.6.0 torchvision==0.21.0 --index-url https://download.pytorch.org/whl/cu124

attrs==25.1.0
better-profanity==0.7.0
boto3==1.35.99
diffusers==0.32.2
einops==0.8.1
huggingface-hub==0.29.2
hydra-core==1.3.2
imageio[pyav,ffmpeg]==2.37.0
iopath==0.1.10
loguru==0.7.2
mediapy==1.2.2
nltk==3.9.1
numpy==1.26.4
nvidia-ml-py==12.535.133
omegaconf==2.3.0
opencv-python==4.10.0.84
pandas==2.2.3
peft==0.14.0
pillow==11.1.0
protobuf==4.25.3
pynvml==12.0.0
pyyaml==6.0.2
retinaface-py==0.0.2
safetensors==0.5.3
scikit-image==0.25.2
sentencepiece==0.2.0
termcolor==2.5.0
tqdm==4.66.5
transformers==4.49.0
